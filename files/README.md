# OpenWrt Files Overlay

This directory will hold OpenWrt `files/` overlay content split by profile.

- `common/` - applies to both profiles.
- `oled/` - OLED services, defaults, and board-specific fixes.
- `performance/` - performance profile additions.
- `full-vpn/` - VPN/Podkop profile additions.

Do not store production secrets here.
