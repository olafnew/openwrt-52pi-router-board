# Patches

Project-maintained patches live here until they are upstreamed or replaced by clean package feeds.

Expected patch areas:

- `luci-app-oled/` - OLED package fixes needed for current OpenWrt packaging and board behavior.
- `r8168/` - Realtek r8168 driver integration and RSS/multi-queue build choices.
